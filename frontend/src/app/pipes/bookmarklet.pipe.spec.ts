import { BookmarkletPipe } from './bookmarklet.pipe';

describe('BookmarkletPipe', () => {
  it('create an instance', () => {
    const pipe = new BookmarkletPipe();
    expect(pipe).toBeTruthy();
  });
});
